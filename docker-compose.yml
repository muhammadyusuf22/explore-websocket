services:
  redis:
    image: redis:7-alpine
    container_name: ws-hub-redis
    restart: unless-stopped
    command: redis-server --requirepass ${REDIS_PASSWORD:-passwordnyaRedis}
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis-data:/data

volumes:
  redis-data:
